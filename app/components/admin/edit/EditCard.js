import React from 'react'

const EditCard = ({id, imageUrl, category, brand, model, quantity, price, description, status, detail, item }) => {

  console.log(item);
  

  return (
    <div>{id}</div>
  )
}; export default EditCard;